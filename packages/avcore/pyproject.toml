[project]
name = "avcore"
version = "0.1.0"
description = "Pure planning core: map model, routing graph, filtering, A* planner"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/avcore"]
